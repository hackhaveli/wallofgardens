export const metadata = {
  title: 'Admin Login — Wall of Gardens',
  robots: 'noindex, nofollow',
};

export default function LoginLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
